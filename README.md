// Instructions for Music Search Program
// Download all files: main.cpp, trie.h, trie.cpp, song.h, HashTable.h, HashTable.cpp, and songs_clean.csv
// Run program in IDE and follow on screen instrcutions
// Search by name, artist, or genre
// Note: When compiling, might see some error messages about ASCII characters because of issues in the database, but the program should run as normal.
